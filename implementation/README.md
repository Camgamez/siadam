There are only three files on this directory:


## Queries:

Some basic queries that can be consulted on the database.

## Schema:

A .SQL file that generates the database using a designed schema.

## Test Data:

Self explanatory. 

---

